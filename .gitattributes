# Auto-detect text files and normalize to LF in repository
* text=auto

# Force LF for these text file types (useful for scripts, code, configs)
*.py     text eol=lf
*.sh     text eol=lf
*.md     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.json   text eol=lf
*.csv    text eol=lf

# Windows batch files should keep CRLF
*.bat    text eol=crlf

# Binary files
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.pdf    binary

# Model and artifact binaries
*.pkl    binary
*.joblib binary
*.h5     binary
*.pt     binary
*.pth    binary
*.ckpt   binary

# Git LFS placeholder (if you use LFS, configure separately)
# *.bin filter=lfs diff=lfs merge=lfs -text
